// This program prints "Hello, world!" to the screen.

// This is a function named `main`.
fn main() {
    // This is a call to the macro `println` (the `!` is because it’s a macro).
    // Its parameter is a string literal to be printed with a newline afterward.
    println!("Hello, world!");
}
